import React, { useEffect, useMemo, useRef, useState } from 'react'
import {
  Box,
  Typography,
  Stack,
  useTheme,
  IconButton,
  Tooltip,
  Divider,
  Button
} from '../../../../../MUI/MuiComponents';
import {
  defaultAvatar,
  ArrowBackIcon,
  ArrowForwardIcon
} from '../../../../../MUI/MuiIcons';

function ProposalPreview(
  { partnerProfile, showProfilePic, myProfile, onClose }
) {
  return (
    <Box>
      I am Preview
    </Box>
  )
};

export default ProposalPreview;